openapi: 3.0.3
info:
  title: Service And Resource Design Studio API
  version: 0.1.0
  description: Bootstrap OpenAPI placeholder. Replace with TMF-aligned command, query, evidence, and admin APIs from dev-tasks.
servers:
  - url: https://api.telcosuite.example/service-and-resource-design-studio/v1
paths:
  /health:
    get:
      operationId: getHealth
      summary: Return service health for deployment smoke tests.
      responses:
        '200':
          description: Service is healthy.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HealthResponse'
components:
  schemas:
    HealthResponse:
      type: object
      required:
        - status
        - app
      properties:
        status:
          type: string
          example: UP
        app:
          type: string
          example: ts-oss-eng-service-and-resource-design-studio
